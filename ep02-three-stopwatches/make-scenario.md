# The Make build (as recorded)

1. New scenario → `+` → **HTTP** → *Make a request*
2. Authentication type: **No authentication**
3. URL: your CSV export URL · Method: **GET**
4. Save → **Run once** → 1 operation, HTTP 200, data flowing (28s from empty canvas)

The pricing logic (Parse CSV → filter won → quarterly math) adds 2–3 more
modules with field mappings — the part the episode honestly did not finish
on camera.
